<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="action_analyze_report_views" model="ir.actions.server">
            <field name="name">Analyze Report Views</field>
            <field name="model_id" ref="base.model_ir_actions_report"/>
            <field name="binding_model_id" ref="base.model_ir_actions_report"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code"><![CDATA[
if not env.context.get('active_id'):
    raise UserError("Please select a report first.\n\nGo to: Settings > Technical > Reports > Select one > Action")

action = env['report.analyzer'].analyze_report(env.context['active_id'])
]]></field>
        </record>
    </data>
</odoo>
